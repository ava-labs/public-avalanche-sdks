{
  "name": "@avalabs/tsup",
  "version": "1.0.0",
  "private": true,
  "main": "tsup.config.ts",
  "scripts": {
    "prettier": "prettier --config ../../.prettierrc --ignore-path ../../.prettierignore --write \"src/**/*.ts*\""
  },
  "devDependencies": {
    "tsup": "7.2.0"
  }
}
